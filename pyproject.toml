[project]
name = "pomobar-documentation"
version = "1.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "mkdocs-material>=9.6.16",
    "mkdocs-minify-plugin>=0.8.0",
]
